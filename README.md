# Arthur Greyf - Стоматологическая клиника

Веб-сайт стоматологической клиники Dr. Arthur Greyf. Многостраничный проект с
современным стеком технологий.

## 🚀 Технологии

- **Webpack 5** - сборщик модулей
- **SCSS/Sass** - препроцессор стилей
- **PostHTML** - обработка HTML шаблонов (extend, include)
- **Swiper** - библиотека для слайдеров
- **Fancybox** - галерея изображений и видео
- **Webpack Dev Server** - сервер для разработки с hot reload

## 📁 Структура проекта

```
arthur-greyf/
├── src/                    # Исходный код
│   ├── assets/            # Ресурсы
│   │   ├── fonts/         # Шрифты (Inter, PT Serif)
│   │   ├── images/        # Изображения
│   │   └── files/         # Файлы (favicon, manifest)
│   ├── scripts/           # JavaScript модули
│   │   ├── components/    # Компоненты (слайдеры, аккордеон, навигация)
│   │   └── index.js       # Точка входа
│   ├── styles/            # SCSS стили
│   │   ├── base/          # Базовые стили (normalize, globals)
│   │   ├── blocks/        # Стили блоков и страниц
│   │   ├── components/    # Стили компонентов
│   │   ├── layout/        # Стили шапки, футера, навигации
│   │   ├── mixins/        # SCSS миксины
│   │   ├── pages/         # Стили страниц
│   │   ├── utils/         # Утилиты (переменные, шрифты)
│   │   └── vendors/       # Стили сторонних библиотек
│   └── views/             # HTML шаблоны
│       ├── blocks/        # Блоки страниц
│       ├── layout/        # Шаблоны layout (header, footer, nav)
│       └── pages/         # Страницы сайта
├── dist/                  # Собранная версия (генерируется при сборке)
├── webpack.config.js      # Конфигурация Webpack
└── package.json           # Зависимости и скрипты
```

## 📄 Страницы сайта

- **Главная** (`index.html`)
- **О докторе** (`dr-arthur-greyf.html`)
- **Услуги** (`services.html`)
- **Цены** (`pricing.html`)
- **Команда** (`meet-the-team.html`)
- **Блог** (`blog.html`, `blog-detail.html`)
- **Отзывы** (`testimonials.html`)
- **FAQ** (`faq.html`)
- **Контакты** (`contact-us.html`)
- **Финансовые опции** (`financial-options.html`)
- **Имплант-поддерживаемые протезы** (`implant-supported-dentures.html`)
- **Визиты в офис** (`office-visits.html`)
- **Награды за рекомендации** (`referral-rewards.html`)
- **Что нас отличает** (`what-sets-us-apart.html`)
- **Квиз** (`quiz.html`)
- **CEREC** (`CEREC.html`)

## 🛠 Установка и запуск

### Установка зависимостей

```bash
npm install
```

### Разработка

Запуск dev-сервера с hot reload:

```bash
npm run dev
# или
npm start
```

Сервер будет доступен по адресу `http://localhost:8080` (или другой порт, если
8080 занят)

### Сборка проекта

Сборка для production:

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`

## 📝 Скрипты

- `npm run build` - сборка проекта
- `npm run dev` / `npm start` - запуск dev-сервера
- `npm run lint:scss` - проверка SCSS кода линтером
- `npm run lint:fix` - автоматическое исправление ошибок линтера

## 🎨 Компоненты

Проект включает следующие JavaScript компоненты:

- **Header** - управление шапкой сайта
- **Mobile Nav** - мобильная навигация
- **Accordion** - аккордеон для FAQ и других секций
- **Sliders** - различные слайдеры (промо, услуги, отзывы, цены и т.д.)
- **Submenu** - управление подменю
- **Fancybox** - галерея изображений и видео
- **Equalize Financing Rows** - выравнивание высоты строк в финансовых опциях
- **FAQ Detail Anchors** - якорные ссылки для FAQ

## 🌿 Git workflow

### Структура веток

- **`develop`** - ветка разработки с полным исходным кодом проекта
- **`main`** - ветка продакшена с только собранной версией (папка `dist`)

### Рабочий процесс

1. Всегда работайте в ветке `develop`:

   ```bash
   git checkout develop
   ```

2. Вносите изменения в исходный код

3. Собирайте проект для тестирования:

   ```bash
   npm run build
   ```

4. Коммитьте изменения:
   ```bash
   git add .
   git commit -m "Описание изменений"
   ```

### Деплой в продакшен

Для обновления продакшен версии используйте скрипт деплоя (если доступен):

```bash
./deploy.sh
```

Или вручную:

```bash
# На ветке develop
npm run build

# Переключиться на main
git checkout main

# Обновить файлы
git rm -rf dist/
git add -f dist/
git commit -m "Update production build"
```

## 📦 Зависимости

### Production

- `@fancyapps/ui` - галерея изображений и видео
- `swiper` - библиотека слайдеров

### Development

- `webpack` - сборщик модулей
- `webpack-cli` - CLI для Webpack
- `webpack-dev-server` - dev-сервер
- `sass` / `sass-loader` - компиляция SCSS
- `css-loader` - загрузка CSS
- `mini-css-extract-plugin` - извлечение CSS в отдельные файлы
- `html-webpack-plugin` - генерация HTML
- `posthtml-loader` - обработка HTML шаблонов
- `posthtml-extend` - наследование шаблонов
- `posthtml-include` - включение шаблонов
- `stylelint` - линтер для SCSS/CSS

## 👤 Автор

**Arina Terekhina** Email: terekhinaarina00@gmail.com

## 📄 Лицензия

MIT
